/// add 1 to given number and return it
///
/// # Examples
///
/// ```
/// let arg = 5;
/// let answer = doc::add_one(arg);
///
/// assert_eq!(6, answer);
/// ```
pub fn add_one(x: i32) -> i32 {
    x + 1
}

fn main() {}
